# 🛸 **Exercice : Gestion d’une file FIFO dans un bar clandestin intergalactique (version sans POO)**

## 🎬 Contexte narratif

Maelys, stagiaire DevOps du *Nebula Speakeasy*, un bar clandestin perdu dans une station orbitale obscure, doit gérer toutes les **demandes clients** pendant la soirée.

Le problème : le système informatique est *archi-vieux*, et les requêtes doivent être traitées **dans l’ordre exact d’arrivée**.
Aucune priorité, aucune triche, même si un diplomate cyborg claque des crédits en menaçant d’un traité interplanétaire.

Maelys doit donc manipuler une **file FIFO** (First In – First Out) à l’aide d’une simple **liste Python** et d’un ensemble de fonctions.

---

# 🎯 Objectifs pédagogiques

* manipuler une file FIFO uniquement avec une liste ;
* utiliser les opérations :

  * `enqueue` (ajouter en fin),
  * `dequeue` (retirer en tête),
  * `peek` (lire le premier élément),
  * `is_empty` ;
* gérer une simulation complète.

---

# 🧠 Travail demandé

Tu dois implémenter les **fonctions suivantes**, qui manipulent une **liste représentant la file** :

### 1️⃣ Fonctions à écrire

```python
def enqueue(file, element):
    """Ajoute un élément en fin de file."""
    pass

def dequeue(file):
    """Retire et renvoie l’élément en tête de file."""
    pass

def peek(file):
    """Renvoie l’élément en tête sans le retirer."""
    pass

def is_empty(file):
    """Renvoie True si la file est vide."""
    pass

def afficher_file(file):
    """Affiche proprement la file dans l’ordre FIFO."""
    pass
```

---

# 🛰️ 2️⃣ Simulation d’une soirée au Nebula Speakeasy (22h00–23h00)

Crée une liste vide appelée `file_requetes`.

Puis, dans cet ordre précis, ajoute les requêtes suivantes avec `enqueue` :

1. `"Nox le Contrebandier – Recharger son blaster biométrique"`
2. `"Ambassadrice Vekna – Commander un cocktail quantique"`
3. `"Unité Minérale ZR-12 – Commander un verre de silicium liquide"`
4. `"Capitaine Rhaen – Transférer 200 crypto-stellaires"`
5. `"Drone-Serveur 4B – Demande d’auto-réparation d’urgence"`

Ensuite :

* Affiche la file complète.
* Traite (`dequeue`) les **3 premières requêtes**.
* Ajoute ensuite :

  * `"Scientifique Yll – Commander un neutrino stabilisé"`
  * `"Cyborg Diplomatique 3-Δ – Déclarer un incident protocolaire"`
* Affiche :

  * la prochaine requête à traiter (`peek`)
  * l’état final de la file

---

# 🌌 3️⃣ Bonus : protocole de fermeture nocturne

Écris une fonction :

```python
def shutdown_protocol(file):
    """
    Vide complètement la file en affichant :
    'Traitement final : <élément>'
    puis termine par :
    'Le Nebula Speakeasy ferme ses portes. Les étoiles peuvent dormir.'
    """
    pass
```

Cette fonction représente la procédure d’urgence quand une patrouille galactique approche du bar.

---

# 🧾 Squelette complet à remplir

```python
# Fonctions de gestion FIFO

def enqueue(file, element):
    pass

def dequeue(file):
    pass

def peek(file):
    pass

def is_empty(file):
    pass

def afficher_file(file):
    pass


# --- Simulation de la soirée ---

file_requetes = []

# 1. Arrivées initiales
# À compléter

# 2. Affichage de la file
# afficher_file(file_requetes)

# 3. Traitement de 3 requêtes
# À compléter

# 4. Nouvelles requêtes
# À compléter

# 5. Affichages finaux
# print(peek(file_requetes))
# afficher_file(file_requetes)

# BONUS
# shutdown_protocol(file_requetes)
```